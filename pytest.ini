[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -ra
    --strict-markers
    --tb=short
markers =
    ui: end-to-end browser tests (slow)
    api: API tests (fast)
    smoke: critical-path smoke tests
